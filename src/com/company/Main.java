package com.company;

public class Main {

    public static void main(String[] args) {
	// to use another class you need to create an object
        //first class name, class object set eql to new and class name
        //to use it use the object name dot the method name u want to use
        tuna tunaObject = new tuna();
        tunaObject.simpleMessage();
    }
}
